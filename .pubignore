/build/
/.dart_tool/
/coverage/

# Demo GIFs for README only — keep in git, exclude from pub.dev tarball.
/doc/demos/
*.iml
.idea/
.vscode/

# Native runner scaffolding + generated splash/launcher assets are only
# needed to run the example app locally, not to browse it on pub.dev.
# They stay in the GitHub repo; excluding them keeps the published
# archive close to the actual lib/ size.
/example/android/
/example/ios/
/example/linux/
/example/macos/
/example/windows/
/example/web/
/example/assets/
/example/pubspec.lock
